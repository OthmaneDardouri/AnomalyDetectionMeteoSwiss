torch>=2.1
torchvision>=0.16
numpy>=1.24
PyYAML>=6.0
Pillow>=10.0
matplotlib>=3.7
scikit-learn>=1.3
scipy>=1.11
tqdm>=4.66
tensorboard>=2.14
einops>=0.7
torchinfo>=1.8

# Serving only; imported lazily, so training works without them.
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9

# Experiment tracking (--mlflow), deliberately NOT installed by default:
#   pip install "mlflow>=2.9"
